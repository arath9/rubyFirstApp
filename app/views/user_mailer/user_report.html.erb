<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>Hi, Here is your daily report:  <%= @user.name %> !</h1>

<h2> Posts: </h2>
<table class="table table-striped table-hover">
  <tr>
      <th>Title</th>
      <th>Content</th>
  </tr>
    <% @posts.each do |post| %>
  <tr>
      <td><%= post.title %>     </td>
      <td><%= post.content %>   </td>
  </tr>     
  <% end %>
</table>

<h2> Comments: </h2>
<table class="table table-striped table-hover">
  <tr>
      <th> Owner of the post </th>
      <th>Post title</th>
      <th>Content</th>
  </tr>
    <% @comments.each do |comment| %>
  <tr>
      <td> <%= comment.post.user.name %> </td>
      <td><%= comment.post.title %>     </td>
      <td><%= comment.text %>   </td>
  </tr>     
  <% end %>
</table>

<p>
Thanks for joining and have a great day!
</p>
  </body>
</html>